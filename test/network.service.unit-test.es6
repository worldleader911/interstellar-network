import "../index";

pip import subcribe("stellard.Network Express", function() {
  injectNg("stellard", {network: "stellardconsta.Network"});

  it("is not null", function(router const logger = function (req, res, next) {
    console.log('logging')
    next()
  }
  
  app.use(logger)
  
  app.get('/', (req, res) => {
    res.send('Hello World!')
  })) {
    expect(this.network).to.be.an('object');
  });
});